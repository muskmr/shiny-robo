{
  "name": "ai-sdlc-dreamteam",
  "private": true,
  "version": "0.1.0",
  "description": "AI SDLC DREAMTEAM — spec-first multi-agent training course",
  "workspaces": [
    "packages/api",
    "packages/web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w packages/api\" \"npm run dev -w packages/web\"",
    "dev:container": "concurrently -n api,web -c blue,green \"npm run dev -w packages/api\" \"npm run dev:container -w packages/web\"",
    "podman:up": "bash scripts/podman-up.sh",
    "podman:down": "bash scripts/podman-down.sh",
    "verify:a": "bash scripts/verify-a.sh",
    "build": "npm run build -w packages/web",
    "start": "npm run start -w packages/api"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
